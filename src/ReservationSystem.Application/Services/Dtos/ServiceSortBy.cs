namespace ReservationSystem.Application.Services.Dtos;

/// <summary>Fields a service listing can be sorted by.</summary>
public enum ServiceSortBy
{
    CreatedAt = 0,
    Name = 1,
    Price = 2,
}

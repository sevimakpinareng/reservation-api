namespace ReservationSystem.Application.Common;

/// <summary>
/// Marker interface for the Application layer assembly. Used as a reference
/// point for assembly scanning (e.g. registering services, validators, or
/// mediator handlers) in later phases. The Application layer is intentionally
/// a thin skeleton in Phase 1.
/// </summary>
public interface IApplicationMarker;

namespace ReservationSystem.Application.Appointments.Dtos;

/// <summary>Fields an appointment listing can be sorted by.</summary>
public enum AppointmentSortBy
{
    StartTime = 0,
    CreatedAt = 1,
    Status = 2,
}
